import React, { FC } from 'react';
import Heading from './Heading';
import Navbar from './Navbar'

const Header: FC = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;